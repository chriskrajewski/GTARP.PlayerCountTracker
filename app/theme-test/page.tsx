import { ThemeTest } from "@/components/theme-test";

export const metadata = {
  title: "Theme Test - FiveM Player Count Tracker",
  description: "Testing the consistency of the Twitch-like theme"
};

export default function ThemeTestPage() {
  return <ThemeTest />;
} 