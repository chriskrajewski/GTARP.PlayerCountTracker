// Global Jest setup, run once per test file after the test framework is
// installed. Add custom matchers, polyfills, or global mocks here.
//
// Intentionally minimal for now; the property-test helpers live in
// __tests__/pbt-helpers.ts and custom arbitraries in __tests__/arbitraries.ts.

export {}
